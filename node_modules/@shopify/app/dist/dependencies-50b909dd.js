import { u as updateDependencies } from './app-5df50971.js';
import { ui, dependency, environment } from '@shopify/cli-kit';

async function installAppDependencies(app) {
  const list = new ui.Listr([
    {
      title: "Installing any necessary dependencies",
      task: async (_, task) => {
        await dependency.installNPMDependenciesRecursively({
          dependencyManager: app.dependencyManager,
          directory: app.directory,
          deep: 3
        });
        task.title = "Dependencies installed";
      }
    }
  ], { rendererSilent: environment.local.isUnitTest() });
  await list.run();
  return updateDependencies(app);
}

export { installAppDependencies as i };
//# sourceMappingURL=dependencies-50b909dd.js.map
